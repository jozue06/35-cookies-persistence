## josh lab 35 basic auth

### Instructions

##### Download this repo:

Navigate inside the 'client' directory in the repo and run ` npm i` to install dependencies.

After dependencies are installed, run `npm start`.
